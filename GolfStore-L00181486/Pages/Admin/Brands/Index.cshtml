@page
@model GolfStore_L00181486.Pages.Admin.Brands.IndexModel
@{
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h3>Clubs</h3>
        </div>
        <div class="col-6">
            <a asp-page="Create" class="btn btn-primary">
                Create New Club
            </a>
        </div>
    </div>
</div>

<br /><br /><br />

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Abbreviation</th>
            <th>Image</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model.Brands)
        {
            <tr>
                <td>@obj.Name</td>
                <td>@obj.Abbreviation</td>
                <td>
                    <img src="@obj.LogoUrl" alt="@obj.Name" width="50" />
                </td>
                <td>
                    <div class="btn-group" role="group">
                        <a asp-page="Edit" asp-route-id="@obj.BrandId" class="btn btn-primary">Edit</a>
                        <a asp-page="Delete" asp-route-id="@obj.BrandId" class="btn btn-danger">Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>